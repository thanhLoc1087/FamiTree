import 'package:flutter/material.dart';

class ManagePlace extends StatelessWidget {
  const ManagePlace({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('CMS'),);
  }
}